<div class="hero min-h-screen bg-base-200">
  <div class="hero-content text-center">
    <div class="max-w-md">
      <h1 class="text-5xl font-bold">EuFunding</h1>
      <p class="py-6">
        Caută informații despre orice companie din România folosind CUI-ul (Codul Unic de Identificare).
      </p>

      <%= form_with url: company_lookup_path, method: :post, local: true, class: "space-y-4" do |form| %>
        <div class="form-control w-full max-w-xs mx-auto">
          <%= form.label :cui, "CUI Companie", class: "label" %>
          <span class="label-text">Introdu CUI-ul companiei</span>
          <%= form.text_field :cui,
                placeholder: "ex: 14837428",
                class: "input input-bordered w-full max-w-xs",
                required: true,
                pattern: "[0-9]+",
                title: "Introdu doar cifre" %>
        </div>

        <div>
          <%= form.submit "Caută Compania", class: "btn btn-primary" %>
        </div>
      <% end %>

      <div class="text-sm text-gray-500 mt-4">
        <p>Exemplu de CUI pentru testare: <strong>14837428</strong></p>
      </div>
    </div>
  </div>
</div>
