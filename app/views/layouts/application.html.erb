<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "EuFunding - Căutare Informații Companii România" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="description" content="Caută informații detaliate despre orice companie din România folosind CUI-ul. Date actualizate din Registrul Comerțului.">
    <meta name="keywords" content="companii romania, cui, registrul comertului, informatii firme, caen, cifra afaceri">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen bg-base-100">
    <!-- Navigation -->
    <div class="navbar bg-base-100 shadow-lg">
      <div class="navbar-start">
        <%= link_to root_path, class: "btn btn-ghost normal-case text-xl" do %>
          EuFunding
        <% end %>
      </div>
      <div class="navbar-end">
        <%= link_to "API Logs", api_logs_path, class: "btn btn-ghost" %>
      </div>
    </div>

    <%= yield %>
  </body>
</html>
